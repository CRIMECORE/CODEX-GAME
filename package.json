{
  "name": "crimecore-bot",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "local:db": "docker compose -f docker-compose.local.yml up -d",
    "local:db:down": "docker compose -f docker-compose.local.yml down -v",
    "local:start": "npm run local:db && node index.js",
    "restore-data": "node scripts/restoreData.js"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "grammy": "^1.27.0",
    "node-fetch": "^2.7.0",
    "node-telegram-bot-api": "^0.66.0",
    "sharp": "^0.34.3",
  "mysql2": "^3.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
